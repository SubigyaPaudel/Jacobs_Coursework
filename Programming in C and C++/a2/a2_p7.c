/*
CH-230-A
a2_p7.c
Subigya Paudel
spaudel@jacobs-university.de
*/

#include <stdio.h>

int main(){
    int i = 8;
    while (i >= 4){ // adding the curly braces to indicate the codes that need to be repeated
    printf("i is %d\n", i);
    i--;
    }
    return 0;
}
